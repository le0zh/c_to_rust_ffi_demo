[package]
name = "c_to_rust_ffi_demo"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["lib", "staticlib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"